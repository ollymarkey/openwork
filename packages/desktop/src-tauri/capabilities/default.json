{
  "$schema": "https://schemas.tauri.app/capability",
  "identifier": "default",
  "description": "Default capabilities for OpenWork",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
